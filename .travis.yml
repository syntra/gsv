language: node_js
node_js:
  - "lts/*"
  - "node"
  - "8"
install:
  - yarn install
  - npm install -g codecov
script:
  - npm run test
  - nyc report --reporter=text-lcov > coverage.lcov && codecov
  - codecov
